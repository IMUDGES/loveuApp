<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             tools:context=".NoBoringActionBarActivity">

    <ListView
            android:id="@+id/noboring_listview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"/>

    <FrameLayout
            android:id="@+id/noboring_header"
            android:layout_width="match_parent"
            android:layout_height="@dimen/header_height">

        <com.example.loveuApp.User.KenBurnsView
                android:id="@+id/noboring_header_picture"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/picture0"/>

        <ImageView
                android:id="@+id/noboring_header_logo"
                android:layout_width="@dimen/header_logo_size"
                android:layout_height="@dimen/header_logo_size"
                android:layout_gravity="center"
                android:src="@drawable/ic_header_logo"/>

    </FrameLayout>

</FrameLayout>